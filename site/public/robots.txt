User-agent: *
Allow: /

# Astro emits an index that points at the numbered sitemaps. The old site
# published a single sitemap.xml, and that URL kept being advertised here long
# after it stopped existing, so crawlers followed robots.txt straight into a
# 404. /sitemap.xml now redirects here as well, for anything that still asks
# for it by that name.
Sitemap: https://campuscli.com/sitemap-index.xml

# Campus permite la búsqueda y el uso interactivo por agentes, pero no autoriza
# entrenar modelos con su contenido. El catálogo describe los recursos para
# agentes que sí están disponibles públicamente.
Content-Signal: ai-train=no, search=yes, ai-input=yes
Agentmap: https://campuscli.com/.well-known/ard.json
